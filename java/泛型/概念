1.为什么要有泛型
    不确定容器实际存的是什么类型，其他管理操作时确定的，把元素的类型设计成一个参数，就叫泛型;
    比如垃圾分类，标明可回收垃圾，不可回收垃圾，人们就知道往里面扔什么垃圾
2.自定义泛型结构

3.泛型在继承上的体现

类A是类B的父类
G<A> G<B> 是并列的
A<G> B<G> 子父类关系

4.通配符的使用
List<?> a
不允许写入数据

<? extends A> A是上限 <=
<? super A> A是下限   >=
5.泛型应用场景